const loader = () => {
    return (
        <div class="flex absolute inset-0 bg-black z-50 items-center justify-center">
            <div class="w-16 h-16 border-b-2 border-white rounded-full animate-spin"></div>
        </div>
    );
};

export default loader;
